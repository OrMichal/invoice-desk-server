create table if not exists _invoices(
        id bigint auto_increment primary key not null,
        num nvarchar(50) not null
);

create table if not exists _invoice_template(
        id bigint auto_increment primary key not null
);
